development:
  carry_over_reconciliation:
    class: CarryOverReconciliationJob
    cron: "*/10 * * * *"
    queue: default

test: {}

production:
  carry_over_reconciliation:
    class: CarryOverReconciliationJob
    cron: "*/10 * * * *"
    queue: default
